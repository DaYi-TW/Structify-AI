services:
  backend:
    build: ./backend
    ports:
      - "8100:8000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        VITE_BACKEND_URL: http://localhost:8100
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped
